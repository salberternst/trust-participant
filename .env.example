COMPOSE_PROJECT_NAME=trust-participant
COMPOSE_PROJECT_PREFIX=trust

# Network categories:
# - backend: Docker-internal only (Postgres, Vault, init jobs)
# - internal: participant application network for company/private services
# - public: only public gateways and reverse proxies
TRUST_PARTICIPANT_PUBLIC_NETWORK=trust-public
TRUST_PARTICIPANT_PUBLIC_NETWORK_EXTERNAL=false

PARTICIPANT_ROLE=participant
PARTICIPANT_BPN=BPNL00000003AYRE
PARTICIPANT_BPN_BASE64=QlBOTDAwMDAwMDAzQVlSRQ==
# EDC runtime context id. IdentityHub's participant context id defaults to the BPN.
PARTICIPANT_CONTEXT_ID=596b0330-76ec-5c8e-af01-24ba6a21d4e6
IDENTITYHUB_PARTICIPANT_CONTEXT_ID=BPNL00000003AYRE

# Helper images. The compose artifact references published images, not local builds.
PARTICIPANT_INIT_IMAGE=ghcr.io/salberternst/trust-participant-init:latest
PARTICIPANT_PORTAL_GATEWAY_IMAGE=ghcr.io/salberternst/trust-participant-portal-gateway:latest
PARTICIPANT_PORTAL_IMAGE=ghcr.io/salberternst/portal:latest

# Public dataspace URL. This is the only required externally reachable participant URL.
PARTICIPANT_PUBLIC_HOST=participant.external.test
PARTICIPANT_DID_HOST=participant.external.test
PUBLIC_DSP_CALLBACK_ADDRESS=https://participant.external.test/api/v1/dsp
PUBLIC_DATAPLANE_BASE_URL=https://participant.external.test/api/public/
# Leave empty to derive this from PARTICIPANT_PUBLIC_HOST and PARTICIPANT_BPN_BASE64.
PUBLIC_CREDENTIAL_SERVICE_ENDPOINT=
USE_HTTPS_DID_WEB=true

# Internal/company URLs served by the internal gateway.
PARTICIPANT_INTERNAL_HOST=participant.internal.test
PARTICIPANT_PORTAL_INTERNAL_HOST=portal.internal.test

# Participant portal and onboarding defaults.
PORTAL_TITLE=Participant Portal
PORTAL_PUBLIC_EDC_ENDPOINT=
ONBOARDING_DATASPACE_ADMIN_API_URL=http://dataspace-admin:3000/api
ONBOARDING_AUTO_SUBMIT=true
ONBOARDING_ORGANIZATION_NAME=Example Participant
ONBOARDING_REQUESTED_BPN=BPNL00000003AYRE
ONBOARDING_CONTACT_EMAIL=ops@example.test
ONBOARDING_REQUESTED_ROLE=participant
ONBOARDING_DID=did:web:participant.external.test:BPNL00000003AYRE
ONBOARDING_DSP_ENDPOINT=https://participant.external.test/api/v1/dsp
ONBOARDING_CREDENTIAL_SERVICE_ENDPOINT=https://participant.external.test/api/credentials

# Trust services provided by the trust operator.
ISSUER_DID_HOST=issuer.external.test
BPN_ISSUER=BPNL00000003CRHK
BDRS_SERVER_URL=https://issuer.external.test/api/directory

IDENTITYHUB_TAG=0.3.2
TRACTUSX_EDC_TAG=0.12.1
POSTGRES_USER=user
POSTGRES_PASSWORD=password
VAULT_DEV_ROOT_TOKEN_ID=root
EDC_API_KEY=password
IDENTITYHUB_API_KEY=

JAVA_TOOL_OPTIONS=-Xmx256m
OTEL_SDK_DISABLED=true
OTEL_JAVAAGENT_ENABLED=false

# Local bind ports. Use a real reverse proxy for production TLS/public DNS.
PUBLIC_HTTP_BIND=127.0.0.1
PUBLIC_HTTP_HOST_PORT=8080
INTERNAL_HTTP_BIND=127.0.0.1
INTERNAL_HTTP_HOST_PORT=8081
